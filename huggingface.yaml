title: FeedbackCraft AI
emoji: 🎯
colorFrom: purple
colorTo: blue
sdk: gradio
sdk_version: 4.0.0
app_file: app.py
pinned: false
license: mit

# Hardware requirements
compute_requirements:
  cpu: 2
  memory: 8Gi

# Environment variables (set these in HF Spaces settings)
# HF_API_KEY: your_api_key_here
# HF_MODEL_NAME: meta-llama/Meta-Llama-3.1-8B-Instruct

# Python version
python_version: "3.10"

# Build command (optional)
# build_command: pip install -r requirements.txt
